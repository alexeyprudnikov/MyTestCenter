{% extends 'base.html.twig' %}

{% block title %}user list{% endblock %}

{% block body %}
    {% for user in users %}
        <a href="{{ path('user_edit', {id: user.id}) }}">{{ user.email }}</a> [{{ user.roles|join(', ') }}]<br>
    {% endfor %}
{% endblock %}
